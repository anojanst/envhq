# deps
node_modules/
.pnpm-store/

# env / secrets
.env
.env.local
.env*.local

# envhq CLI state (link + per-env sync baseline) — never commit
.envhq/
# pre-rebrand (envsync) CLI state — still ignored in case an old checkout has it
.envsync/
.envsync.json

# next
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log
.vercel
coverage/

# local Claude Code config (dev-server launch config, permission settings)
.claude/settings.local.json
apps/web/.agents/*